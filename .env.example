# Public HTTPS origin or custom domain in front of the R2 bucket.
# The online build appends content-addressed object keys from the performance catalog.
VITE_REFERENCE_AUDIO_BASE_URL=https://assets.piano.2226.love/

# Used only by npm run performance:r2:sync.
R2_BUCKET=piano
